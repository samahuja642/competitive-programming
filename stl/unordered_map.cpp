#include<iostream>
#include<unordered_map>
using namespace std;
int main(){
    unordered_map<string,pair<int,int>>um;
    // IN Unordered maps we can use only those datatypes as key which have hashtable defined in them like int , float ,string ,long long 
    // vector , pair , set have no hashtable defined in them , so we can't use them as key
    // baaki sab same map jaisa but unordered hoga
    return 0;
}